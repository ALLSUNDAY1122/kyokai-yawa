const base='https://allsunday1122.github.io/kyokai-yawa/';
const worksSource=await fetch(`${base}data/works.js?audit=${Date.now()}`).then(response=>response.text());
const sandbox={window:{}};Function('window',worksSource)(sandbox.window);const works=sandbox.window.KYOKAI_WORKS||[];
const series=[['真壁夜話','series/makabe.html'],['黒瀬蒐集録','series/kurose.html'],['榊家異聞','series/sakaki.html'],['境界観測記','series/kansoku.html']];
const pages=[['トップページ',''],...series,...works.map(work=>[work.id,`stories/${work.file}`])];
const assets=[['保存管理CSS','data/saved-stories.css','text/css'],['保存管理JavaScript','data/saved-stories.js','javascript'],['トップ検索JavaScript','data/archive-tools.js','javascript'],['シリーズ検索JavaScript','data/series-archive-tools.js','javascript'],['個別化JavaScript','data/home-personalization.js','javascript'],['読了管理JavaScript','data/reading-status.js','javascript']];
const errors=[];const warnings=[];const rows=[];const times=[];let styled=0,scripted=0,storyIds=0;
const get=async(label,path,expected)=>{const started=Date.now();const response=await fetch(`${base}${path}?audit=${Date.now()}`,{headers:{'cache-control':'no-cache'}});const elapsed=Date.now()-started;times.push(elapsed);const type=response.headers.get('content-type')||'';const text=await response.text();rows.push({label,http:response.status,type,time:elapsed});if(!response.ok)errors.push(`${label}: HTTP ${response.status}`);if(expected&&!type.includes(expected))errors.push(`${label}: Content-Typeが不正です（${type}）`);return text;};
for(const [label,path] of pages){const html=await get(label,path,'text/html');if((html.match(/\/data\/saved-stories\.css/g)||[]).length===1)styled++;else errors.push(`${label}: 保存管理CSS参照が1件ではありません`);if((html.match(/\/data\/saved-stories\.js/g)||[]).length===1)scripted++;else errors.push(`${label}: 保存管理JavaScript参照が1件ではありません`);if(path===''){if(!html.includes('data-personal-saved'))errors.push('トップページ: あとで読む個別化欄がありません');if(html.indexOf('saved-stories.js')>html.indexOf('home-personalization.js'))errors.push('トップページ: 保存管理の読み込み順が不正です');}else if(path.startsWith('series/')){if(html.indexOf('saved-stories.js')>html.indexOf('series-archive-tools.js'))errors.push(`${label}: 保存管理の読み込み順が不正です`);}else{const work=works.find(item=>path.endsWith(item.file));if(work&&html.includes(`data-story-id="${work.id}"`))storyIds++;else errors.push(`${label}: 作品IDが一致しません`);}}
const assetText={};for(const [label,path,type] of assets)assetText[path]=await get(label,path,type);
const savedJs=assetText['data/saved-stories.js']||'';for(const token of ['kyokai-yawa-saved-stories-v1','getSavedIds','setSaved','data-toggle-saved','localStorage.setItem'])if(!savedJs.includes(token))errors.push(`本番保存管理処理がありません: ${token}`);
for(const risky of ['fetch(','sendBeacon','XMLHttpRequest','WebSocket'])if(savedJs.includes(risky))errors.push(`本番保存管理JavaScriptに外部通信処理があります: ${risky}`);
if(!(assetText['data/archive-tools.js']||'').includes('あとで読むだけ'))errors.push('トップ一覧の保存絞り込みがありません');
if(!(assetText['data/series-archive-tools.js']||'').includes('あとで読むだけ'))errors.push('シリーズ一覧の保存絞り込みがありません');
if(!(assetText['data/home-personalization.js']||'').includes('data-personal-saved'))errors.push('個別化欄の保存作品処理がありません');
if(styled!==53||scripted!==53)errors.push(`本番共通資産が53ページではありません（CSS ${styled}/JS ${scripted}）`);if(storyIds!==48)errors.push(`本番作品IDが48件ではありません（${storyIds}件）`);
const sorted=[...times].sort((a,b)=>a-b);const median=sorted[Math.floor(sorted.length/2)]||0;const p95=sorted[Math.min(sorted.length-1,Math.ceil(sorted.length*.95)-1)]||0;
const report=['# 境界夜話 本番あとで読む管理監査','',`- 実行日時: ${new Date().toISOString()}`,`- 公開ページ: ${pages.length}/53`,`- 共通資産: ${assets.length}`,`- CSS参照: ${styled}/53`,`- JavaScript参照: ${scripted}/53`,`- 作品ページID: ${storyIds}/48`,'- 保存方式: ブラウザー端末内localStorage','- 外部送信: なし',`- エラー: ${errors.length}`,`- 警告: ${warnings.length}`,`- 応答時間中央値: ${median}ms`,`- 応答時間p95: ${p95}ms`,'','## エラー','',...(errors.length?errors.map(error=>`- ${error}`):['- なし']),'','## 警告','',...(warnings.length?warnings.map(warning=>`- ${warning}`):['- なし']),'','## 配信確認（主要）','','| 対象 | HTTP | Content-Type | 応答 |','|---|---:|---|---:|',...rows.filter((_,index)=>index<5||index>=pages.length).map(row=>`| ${row.label} | ${row.http} | ${row.type} | ${row.time}ms |`),''].join('\n');
await import('node:fs').then(fs=>{fs.mkdirSync('reports',{recursive:true});fs.writeFileSync('reports/live-saved-stories-audit.md',report);});console.log(report);if(errors.length)process.exitCode=1;