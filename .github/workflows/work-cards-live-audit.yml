name: Live Work Cards Audit

on:
  workflow_run:
    workflows: ['Work Cards']
    types: [completed]
  workflow_dispatch:

concurrency:
  group: live-work-cards-main
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  audit-live:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Audit production homepage story cards
        run: node scripts/audit-live-work-cards.mjs

      - name: Commit production audit report
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/live-work-cards-audit.md
          if git diff --cached --quiet; then
            echo "Production story card audit is unchanged."
          else
            git commit -m "chore: record homepage story card production audit"
            git pull --rebase origin main
            git push
          fi
