name: Content Audit

on:
  push:
    paths:
      - 'stories/**'
      - 'data/works.js'
      - 'scripts/audit-stories.mjs'
      - '.github/workflows/content-audit.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Audit story metadata and reading times
        id: audit
        continue-on-error: true
        run: node scripts/audit-stories.mjs

      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: content-audit-report
          path: reports/content-audit.md
          if-no-files-found: error

      - name: Commit updated audit report
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/content-audit.md
          if git diff --cached --quiet; then
            echo "Audit report is unchanged."
          else
            git commit -m "chore: update content audit report"
            git push
          fi

      - name: Fail when audit errors exist
        if: steps.audit.outcome == 'failure'
        run: exit 1
