name: Live Home Personalization Audit

on:
  workflow_run:
    workflows: ['Home Personalization']
    types: [completed]
  workflow_dispatch:

concurrency:
  group: live-home-personalization-main
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  audit-live:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Wait for GitHub Pages deployment
        run: sleep 20
      - name: Audit production personalized entry points
        run: node scripts/audit-live-home-personalization.mjs
      - name: Commit production audit report
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/live-home-personalization-audit.md
          if git diff --cached --quiet; then
            echo "Production personalization audit is unchanged."
          else
            git commit -m "chore: record personalized homepage production audit"
            git pull --rebase origin main
            git push
          fi
